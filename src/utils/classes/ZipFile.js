class ZipFile {
  static unzipTo(src, dest) {
    // Implementation for unzipping the specified file to the specified folder
    // Add your code here
  }

  static zipTo(src, dest) {
    // Implementation for zipping the specified folder content to the specified file
    // Add your code here
  }
}

global.ZipFile = ZipFile;
module.exports = ZipFile;
